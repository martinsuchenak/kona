# Optional, for the token benchmark. Without it benchmark_tokens.py falls back
# to a vocabulary-free estimator that materially overstates Kona's advantage
# (it reported +1.8% vs terse English where the real figure is +61.6%).
#
#   python3 -m venv bench-env
#   ./bench-env/bin/python -m pip install -r requirements-dev.txt
#   ./bench-env/bin/python benchmark_tokens.py
tiktoken>=0.7
